import React from 'react';

const App: React.FC = () => {
  return (
    <div className="text-center text-2xl font-bold text-blue-500">
      Welcome to React + Vite + Tailwind + TypeScript!
    </div>
  );
};

export default App;
